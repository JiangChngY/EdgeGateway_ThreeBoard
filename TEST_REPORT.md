# 软件验收报告

验收日期：2026-08-09

## 已完成并通过

- 最终交付目录执行自动测试：21项全部通过。
- 公共C协议使用WSL GCC 13.3，以 `-std=c99 -Wall -Wextra -Werror -pedantic` 严格编译通过；`tests/c_protocol_test.c` 运行通过。
- 三端工程目录、公共协议、配置文件、部署脚本和项目文档均已生成。
- Python 协议参考实现通过 CRC16、传感器帧编解码、分片/噪声流解析、坏 CRC 后恢复测试。
- TCP 参考链路通过 NDJSON 上报、服务端接收和 ACK 应答测试。
- STM32 Keil 工程文件可被 XML 解析，工程中引用的源码文件均存在。
- MP157 与 i.MX6ULL 的 qmake 工程中引用的源码文件均存在。
- 一键测试还会校验必需交付文件、Keil 设备与宏定义、Keil/qmake 源文件引用、Python 语法、INI 配置和 Linux 脚本换行符。
- 参考 i.MX6ULL 服务器与 MP157 上报模拟器进行了3帧实际进程间联调，序号1、2、3均收到同序号 `ok:true` ACK。

执行方式：

```text
双击“一键电脑测试.bat”
```

或在项目根目录执行：

```text
python tests\run_all.py
```

## 当前环境无法代替完成的验证

- STM32F103 工程尚未使用 Keil/ARMCC 实际编译、下载到开发板。
- MP157 与 i.MX6ULL 工程尚未在各自的 ARM Linux SDK 中交叉编译。
- 串口、电阻式触摸屏、网线、路由器以及三块开发板之间的实物联调尚未进行。

这些属于上板阶段，不是源码缺失。建议严格按照 `docs/03` 至 `docs/07` 的顺序逐步验证，并在 `docs/上板验证记录.md` 中记录结果。

## 交付结论

当前交付物已经达到“代码齐全、电脑侧协议链路可运行、部署步骤明确、可进入上板调试”的状态。不能在未连接硬件和未安装厂商工具链的情况下宣称已完成真实硬件验收。
